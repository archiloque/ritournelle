def Int add_two(Int number)
  return number.plus(2)
end

def Float add_two(Float number)
  return number.plus(2.0)
end

def Int four()
  Int f = 4
  return f
end

Int i = 10
Int j = self.add_two(i)
Int k = self.add_two(10)

Float l = 10.0
Float m = self.add_two(l)
Float n = self.add_two(10.0)