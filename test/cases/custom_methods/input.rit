def Integer add_two(Integer number)
  return number.plus(2)
end

def Float add_two(Float number)
  return number.plus(2.0)
end

def Integer four()
  Integer f = 4
  return f
end

Integer i = 10
Integer j = self.add_two(i)
Integer k = self.add_two(10)

Float l = 10.0
Float m = self.add_two(l)
Float n = self.add_two(10.0)